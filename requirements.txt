# Core dependencies (pinned to minimum tested versions)
torch>=2.0
numpy>=1.24
pandas>=2.0
pyarrow>=12.0
matplotlib>=3.7
scikit-learn>=1.3
pyyaml>=6.0
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0

# Optional: model benchmarking
torchvision>=0.15
transformers>=4.30

# Optional: profiling
torch-tb-profiler>=0.4

# Optional: Triton kernels (Linux only)
# triton>=2.1.0

# Optional: Rich CLI formatting
rich>=13.0

# Dev / CI
pytest>=7.0
pytest-cov
